//while

let index = 1
while (index <=20) {
    console.log(`Value is ${index}`);
    index = index + 2
}

let arr = ['Me', 'Myself', 'I']
let index1 = 0
while (index1 < arr.length) {
    console.log(`It is ${arr[index1]}`);
    index1 = index1 + 1
}